public class BookAddPrefix {

    // klasa z metoda zwracajaca Stringa w postaci prefixu i autora, jest tworzona na podstawie wczesniej
    // wymyslonego do niej testu
    public String addPrefixToAuthor (String prefix, String author) {
        return prefix + " " + author;
    }
}
